[project]
name = "nmap-risk-engine"
version = "0.1.0"
description = "Python workflow for Nmap scanning, recon enrichment, risk scoring, and reporting"
requires-python = ">=3.10"

[tool.unittest]
start-directory = "tests"
